[build]
builder = "RAILPACK"

[deploy]
# Railpack runtime has `uv` + project venv. $PORT is injected by Railway.
startCommand = "sh -c 'uv run uvicorn prospective_memory.api:app --host 0.0.0.0 --port ${PORT:-8080}'"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 8
