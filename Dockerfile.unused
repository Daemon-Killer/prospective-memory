# Unused when railway.toml builder = RAILPACK. Kept as fallback.
FROM python:3.12-slim

WORKDIR /app
COPY pyproject.toml README.md ./
COPY src ./src
COPY start.py ./
RUN pip install --no-cache-dir .

ENV PORT=8080
EXPOSE 8080
CMD ["python", "start.py"]
